# CodeFuson_web

## Run project
Run Django:
`python manage.py runserver `

Run React:
`npm start`

Generate requirements:

    -Navigate backend
    -pip freeze > requirements.txt


Build docker:
`docker-compose up --build`

Run docker:
`docker-compose up`

## Pages:

- admin